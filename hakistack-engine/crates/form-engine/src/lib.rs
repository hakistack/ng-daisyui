//! DEFERRED — placeholder for `hk-dynamic-form`'s condition / validation engine.
//!
//! See `crates/form-engine/README.md` for the rationale. This crate exists so
//! the workspace stays symmetric and the CI build smoke-tests its slot;
//! actually implementing it should wait until `hk-dynamic-form` proves a need.
